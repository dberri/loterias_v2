# Capy — index da memória (auto-gerado)
> Catálogo determinístico regenerado pelo Capy a cada escrita (T79). Modelo tiered: **hot → index → página**. Ler `hot.md` primeiro. · atualizado 2026-07-15

## Camadas
- **hot.md** — onde paramos agora (retomada 1s). Ler primeiro.
- **decisions.md** — decisões datadas + porquê (append-only).
- **roadmap/** — cards (1 arquivo = 1 item, frontmatter `status`). 0 cards.
- **sessions/** — digests de sessão. 4 arquivos.
- **archive/** — histórico congelado read-only. 0 arquivos.
